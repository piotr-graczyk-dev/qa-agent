# QA Hints, not a test DSL

The v1 QA Agent accepts natural-language QA Hints to guide exploratory mobile checks, but it does not define a deterministic test-step DSL. This keeps the product focused on lightweight black-box PR QA instead of competing with established mobile E2E frameworks.
