# Local debug mode

The v1 QA Agent includes a local debug path that runs against an already launched simulator or emulator using the same config and report contract as CI. Local mode does not build, install, or provision devices; it exists to debug agent behavior, auth profiles, and reports without waiting for a full EAS workflow.
