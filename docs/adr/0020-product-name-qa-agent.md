# Product name is QA Agent

The public product and package identity should be `qa-agent` rather than Expo-specific naming. v1 still supports only Expo/EAS, but the neutral name leaves room for future adapters without renaming the project.
