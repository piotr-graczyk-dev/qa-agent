# Advisory QA by default

The v1 QA Agent reports findings as a pull request comment by default instead of acting as a mandatory merge gate. Teams can opt into Strict Mode once the signal is trusted, but the default behavior avoids turning probabilistic mobile UI exploration into a flaky blocker.
