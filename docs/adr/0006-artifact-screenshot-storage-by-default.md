# Artifact screenshot storage by default

The v1 QA Agent stores screenshots as workflow artifacts by default, avoiding required third-party storage for the first setup. Projects can opt into Vercel Blob for inline pull request images, with provider interfaces left open for S3, R2, or GCS later.
