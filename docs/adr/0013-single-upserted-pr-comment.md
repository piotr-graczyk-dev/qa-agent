# Single upserted PR comment

The v1 QA Agent renders one pull request comment identified by a stable marker and updates it on subsequent runs. This keeps the latest QA Report visible without spamming reviewers, while detailed run history remains in workflow artifacts or a future hosted mode.
