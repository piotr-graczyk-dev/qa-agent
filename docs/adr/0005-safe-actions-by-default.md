# Safe actions by default

The v1 QA Agent defaults to a `safe_only` Action Safety Policy, allowing navigation, login, inspection, and screenshots while avoiding destructive or externally visible actions. Projects may opt into `allow_project_actions` with explicit allowed and forbidden intents for controlled QA environments.
