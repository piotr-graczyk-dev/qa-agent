# Contract evals first

The v1 QA Agent starts with contract evals around report generation, action safety, auth secrecy, comment rendering, and status classification. Full mobile visual evals can be added later once the core agent contract is stable and the example app provides reliable dogfooding coverage.
