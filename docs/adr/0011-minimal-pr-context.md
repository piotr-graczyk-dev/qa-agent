# Minimal PR context

The v1 QA Agent uses a small PR Context artifact rather than full source diffs by default. This gives the agent enough intent to guide Black-Box QA while keeping token usage, privacy exposure, and code-review scope under control.
