# agent-device only in v1

The v1 QA Agent uses `agent-device` as its only Mobile Device Driver. Supporting one backend keeps the first Expo/EAS integration narrow and reliable, while the model-facing tools can remain generic enough to support other automation backends later.
