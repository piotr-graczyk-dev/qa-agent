# Include an example Expo app

The v1 repository includes a small Expo example app for dogfooding the initializer, doctor, local debug mode, auth profiles, screenshot capture, and report rendering. The example should stay intentionally small and predictable rather than becoming a showcase application.
