# Explicit model configuration

The v1 QA Agent requires projects to configure the model provider, model id, and API key environment variable explicitly. The initializer may offer presets, but the runtime should not hide provider choice, data routing, or model cost behind an implicit production default.
